<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorPrimaryBackground"
    >

    <RelativeLayout
        android:id="@+id/action_bar"
        style="@style/TopBarStyle"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/alarm_title"
        >

        <ImageButton
            android:id="@+id/back_button"
            style="@style/BackButtonStyle"
            android:onClick="backToMain" />

    </RelativeLayout>


    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorPrimaryBackground"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            >

            <RelativeLayout
                style="@style/TopLayout"
                android:layout_width="match_parent"
                android:layout_height="@dimen/detail_list_thick_height"
                android:paddingBottom="@dimen/match_constraint"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                >

                <TextView
                    android:id="@+id/calenderMonthnDay"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentStart="true"
                    android:layout_marginLeft="@dimen/basic_margin"
                    android:layout_marginRight="10dp"
                    android:textColor="@color/colorString"
                    android:textStyle="bold"
                    android:textSize="30sp"
                    android:text="MM / DD"
                    />

                <TextView
                    android:id="@+id/calenderYear"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignBottom="@id/calenderMonthnDay"
                    android:layout_toEndOf="@id/calenderMonthnDay"
                    android:textStyle="bold"
                    android:textSize="20sp"
                    android:text="YEAR"/>

            </RelativeLayout>

            <com.prolificinteractive.materialcalendarview.MaterialCalendarView
                android:id="@+id/calendarView"
                style="@style/RoundLayout"
                android:layout_height="wrap_content"
                app:mcv_selectionColor="@color/colorPrimary"
                app:mcv_showOtherDates="all"
                app:mcv_tileWidth="match_parent"/>

            <ListView
                android:id="@+id/calendarListView"
                style="@style/RoundLayout"
                android:layout_height="wrap_content"
                android:layout_marginVertical="@dimen/basic_margin"
                android:listSelector="@android:color/transparent"
                android:divider="@null"
                android:visibility="gone" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>